// Generated by psc-bundle 0.11.6
var PS = {};
(function(exports) {
    'use strict';

  exports.loadData = function(url, id) {
    return function() {
      const request = new XMLHttpRequest();
      request.open('GET', url, true);

      request.onload = function() {
        if (request.status >= 200 && request.status < 400) {
          const data = JSON.parse(request.responseText);
          const packages = Object.keys(data).join("\n");

          const container = document.getElementById(id);
          const pre = document.createElement("pre");
          const content = document.createTextNode(packages);

          pre.appendChild(content);
          container.appendChild(pre);
        } else {
          console.error("Some kind of request error", request);
        }
      };

      request.onerror = function() {
        console.error("Big time error", request);
      };

      request.send();
    };
  };
})(PS["Main"] = PS["Main"] || {});
(function(exports) {
    "use strict";
  var $foreign = PS["Main"];
  var Control_Bind = PS["Control.Bind"];
  var Control_Monad_Eff = PS["Control.Monad.Eff"];
  var DOM = PS["DOM"];
  var Data_Function_Uncurried = PS["Data.Function.Uncurried"];
  var Data_Semigroup = PS["Data.Semigroup"];
  var Data_Unit = PS["Data.Unit"];        
  var packageSet = function (set) {
      return "https://cdn.rawgit.com/joneshf/purescript-package-sets/" + (set + "/packages.json");
  };
  var main = function __do() {
      $foreign.loadData(packageSet("psc-0.11.6"), "psc-container")();
      return $foreign.loadData(packageSet("purerl-0.11.6"), "purerl-container")();
  };
  exports["main"] = main;
})(PS["Main"] = PS["Main"] || {});
PS["Main"].main();
